PORT=4000
DATABASE_URL=postgres://bingo:bingo@localhost:5432/bingo
JWT_SECRET=change_this_to_a_long_random_secret
CORS_ORIGIN=http://localhost:5173

# Bingo economics
PLATFORM_FEE_PERCENT=25
STAKE_TIERS=10,50,100,200
CALL_INTERVAL_MS=2500
STARTING_VIRTUAL_BALANCE=0

# First admin account (created by migrate script if not present)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change_this_password

# Render deployment note:
# Set DATABASE_URL, JWT_SECRET, CORS_ORIGIN, ADMIN_PASSWORD in the Render dashboard.
